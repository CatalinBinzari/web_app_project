# web_app_project

go test -cover
go run .\cmd\web\
go test -coverprofile=coverage && go tool cover -html=coverage

used soda to connect to Db:
![Screenshot](database_schema_pic.png)
soda generate fizz CreateUserTable
soda migrate